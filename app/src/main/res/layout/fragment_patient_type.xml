<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/buttons_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <Button
        android:id="@+id/add_new_patient_button"
        style="@style/SubmitButton.Green"
        android:text="@string/fragment_patient_type_add_new_title" />

    <Button
        android:id="@+id/use_existing_patient_button"
        style="@style/SubmitButton.Green"
        android:layout_marginTop="15dp"
        android:text="@string/fragment_patient_type_use_existing_title" />
</LinearLayout>