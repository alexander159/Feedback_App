<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="EditText.Style" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/EditText.TextColour</item>
        <!-- textColorHint not working with TextInputLayout as parent -->
        <item name="android:textColorHint">@color/EditText.TextColourHint</item>
        <item name="android:textSize">8pt</item>
        <item name="android:gravity">left|center_vertical</item>
        <item name="android:singleLine">true</item>
        <!-- Layout attributes. These will only apply if used directly in view -->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <!-- theme for bottom line color -->
        <item name="android:theme">@style/EditText.Theme</item>
    </style>

    <style name="EditText.Theme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Bottom line when not selected / focused -->
        <item name="colorControlNormal">@color/EditText.Normal</item>
        <!-- Bottom line when selected / focused -->
        <item name="colorControlActivated">@color/EditText.Activated</item>
        <item name="colorControlHighlight">@color/EditText.Activated</item>
    </style>

    <style name="EditText.TexiInputLayout.Theme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/EditText.TextColourHint</item>
        <!-- Label color in TRUE state -->
        <item name="colorControlActivated">@color/EditText.Activated</item>
    </style>

    <style name="CheckBoxCustomStyle" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:textColor">@color/grey</item>
        <item name="android:button">@drawable/checkbox_bg</item>
        <item name="android:paddingLeft">7dp</item>
        <item name="android:textSize">8pt</item>
    </style>

    <style name="SubmitButton.Green" parent="Widget.AppCompat.Button.Colored">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:background">@drawable/green_button_bg</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:singleLine">true</item>
    </style>

</resources>
